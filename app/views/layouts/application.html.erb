<!DOCTYPE html>
<html>
  <head>
    <title>Airbnb</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
      




    <!-- <nav class="navbar navbar-inverse"> -->
        <!-- <div class="container-fluid" >  -->
       <!--  <div class="navbar-header">   -->
        <div class="col-sm-4">
            <ul class="nav navbar-nav" id="middle-navbar">            
                  <% if current_user != nil %>
                     <li><%= image_tag current_user.avatar.thumb.url, class:"avatar-image thick-green-border"%></li>
                    <li><h3 style="color: brown">Welcome, <%=current_user.username%></h3></li>
                  <%else%> 
                    <li><h3>WELCOME GUEST</h3>
                      <img class="smaller-image thick-green-border" src="/assets/user_default.png"></li>
                  <% end %>
                  </div>
                  

                  <div class="col-sm-4">
                  <div class="row" id="middle-navbar">
                
                    
                   
                    <a href="#" style="font-size: 20px;">My Profile</a>
                   
                  
                      <% if signed_in? -%>
                        <%= link_to t(".sign_out"), sign_out_path, method: :delete %>
                      <% else -%>
                        <%= link_to t(".sign_in"), sign_in_path %>
                      <% end -%>

                      </div>
                      </div>
            </ul>
            <div class="col-sm-4">
            <form class="navbar-form navbar-left">
              <div class="input-group">
                <input type="text" class="form-control" placeholder="Search">
                  <div class="input-group-btn">
                    <button class="btn btn-default" type="submit">
                      <i class="glyphicon glyphicon-search"></i>
                    </button>
                  </div>
              </div>  
            </form>
           <!--  </div> -->
       </div> 
    
 </div> 
    </nav>

  <div id="flash">
    <% flash.each do |key, value| -%>
      <div id="flash_<%= key %>"><%=h value %></div>
    <% end %>
  </div>

<% if signed_in? -%>
<%= link_to 'edit', edit_user_path(current_user) %>
<% end %>

    <%= yield %>
    
  </body>
</html>
